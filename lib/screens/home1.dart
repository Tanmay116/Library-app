import 'package:flutter/material.dart';

Widget buildPage() => Center(
                    child: Text(
                      'No Bookmarks',
                      style: TextStyle(fontSize: 30),
                    ),
                  );
